# runs if this core datapack is the newest version

#run if load status matches this core datapacks version
execute if score #healthindications load.status matches 3 run function healthindications.3:health_bar/trigger_health_bar